# The tool's own generated store — derived data, never commit it.
roman.db
roman.db-shm
roman.db-wal

# Local env / secrets
.env
auth.json
youtube_cookies.txt
feedback.jsonl

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Backups + generated ops logs (live on the box, never in git)
backups/
docs/BACKUP_LOG.md
docs/RESTORE_LOG.md

# secrets and user uploads (never commit)
video_tool.env
auth.json
uploads/

# ---- SAFETY (added 2026-09-02): code is open source, these NEVER are ----
# Credential / secret files and any backup copy of them
auth.json*
*.env
*secret*
youtube_cookies.txt*

# Databases — transcript + corpus data stays on the server only
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Logs (may contain titles, paths, user data)
*.log

# Editor / migration scratch backups (some contain credentials)
*.bak
*.bak-*
*.bak_*

# User-uploaded audio
uploads/
